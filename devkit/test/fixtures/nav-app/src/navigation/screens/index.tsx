// Test fixture: the app's screen components. The generated navigation files
// import these by name from `./screens`. They are plain components — the
// fixture exists only so the generated `navigation.*.tsx` typecheck against
// real screen modules.
export function Home() {
  return null;
}

export function ProductDetail() {
  return null;
}

export function Profile() {
  return null;
}
